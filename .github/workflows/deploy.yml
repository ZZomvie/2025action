name: github action start

on:
  push:
    branches:
      - main

jobs:
  my-deploy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: hell world
        run: echo "hell world"
